<h1>🛒 買い物リスト</h1>
<!-- 通常購入 -->
<details open style="margin-bottom: 20px; border: 1px solid #ddd; padding: 10px; border-radius: 8px;">
  <summary style="font-size: 1.2rem; cursor: pointer; font-weight: bold;">🛒 通常購入</summary>
  <%= button_to "☑️リセット", bulk_update_items_path(kind: "regular"), 
                method: :patch, 
                style: "font-size: 11px; color: #666; background: #f8f9fa; border: 1px solid #ddd; border-radius: 4px; padding: 2px 8px; margin: 10px 0; cursor: pointer;" %>
  <!-- 💥 作成した部品を呼び出す（kind: "regular" を渡す） -->
  <%= render "items/form", item: Item.new, kind: "regular", category: @group.categories.find_by(name: "通常購入") %>
  <div id="regular_items" style="margin-top: 10px;">
    <% if @regular_items.present? %>
      <%= render @regular_items %>
    <% else %>
      <p style="font-size: 0.8rem; color: gray;">アイテムがありません</p>
    <% end %>
  </div>
</details>
<!-- 定期購入　-->
<details open style="margin-bottom: 20px; border: 1px solid #ddd; padding: 10px; border-radius: 8px;">
  <summary style="font-size: 1.2rem; cursor: pointer; font-weight: bold;">🗓 定期購入</summary>
  <%= button_to "☑️リセット", bulk_update_items_path(kind: "subscription"), 
                method: :patch, 
                style: "font-size: 11px; color: #666; background: #f8f9fa; border: 1px solid #ddd; border-radius: 4px; padding: 2px 8px; margin: 10px 0; cursor: pointer;" %>
  <!-- 💥 部品を呼び出す（kind: "subscription" を渡す） -->
  <%= render "items/form", item: Item.new, kind: "subscription", category: @group.categories.find_by(name: "定期購入") %>

  <div id="subscription_items" style="margin-top: 10px;">
    <% if @subscription_items.present? %>
      <%= render @subscription_items %>
    <% else %>
      <p style="font-size: 0.8rem; color: gray;">アイテムがありません</p>
    <% end %>
  </div>
</details>

<!-- スポット購入 -->
<details open style="margin-bottom: 20px; border: 1px solid #ddd; padding: 10px; border-radius: 8px;">
  <summary style="font-size: 1.2rem; cursor: pointer; font-weight: bold;">📍 スポット購入</summary>
  <%= button_to "☑️リセット", bulk_update_items_path(kind: "spot"), 
                method: :patch, 
                style: "font-size: 11px; color: #666; background: #f8f9fa; border: 1px solid #ddd; border-radius: 4px; padding: 2px 8px; margin: 10px 0; cursor: pointer;" %>
  <!-- 💥 部品を呼び出す（kind: "spot" を渡す） -->
  <%= render "items/form", item: Item.new, kind: "spot", category: @group.categories.find_by(name: "スポット購入") %>
  <div id="spot_items" style="margin-top: 10px;">
    <% if @spot_items.present? %>
      <%= render @spot_items %>
    <% else %>
      <p style="font-size: 0.8rem; color: gray;">アイテムがありません</p>
    <% end %>
  </div>
</details>

<hr>
<div style="margin-top: 30px;">
  <h2>📅 次回の買い出し予定（AI予測）</h2>
  
  <%= month_calendar(events: @calendar_events) do |date, events| %>
    <div style="min-height: 60px;">
      <%= date.day %>
      <% events.each do |event| %>
        <div style="font-size: 10px; background: #e3f2fd; border-radius: 3px; padding: 2px; margin-top: 2px; color: #1976d2;">
          <%= event.name %>
        </div>
      <% end %>
    </div>
  <% end %>
  <h2>💰 今月の出費内訳</h2>
  <%= pie_chart @expense_chart_data, prefix: "¥", empty: "データがまだありません" %>
</div>
